# Synthetic full-build source

These two tiny uncompressed TSV templates are converted to deterministic
single-member gzip files by integration tests and executable specs. They cover
ascending and descending genes, an overlap, one source `REF=N` exception, and
an ordinary locus on either side of that exception. The sibling FASTA is
synthetic test data, not GRCh38 evidence.
