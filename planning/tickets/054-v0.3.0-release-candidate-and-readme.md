# 054 — Prepare the v0.3.0 application release candidate

Status: complete

## Why

Current `main` contains the safe uninstall command and measured deployment
evidence, but published application version `0.2.0` cannot deliver those
changes. The README is accurate but still longer and more repetitive than the
ordinary install/use path needs to be. This ticket creates one coherent
`0.3.0` application candidate whose version surfaces, user guide, release
notes, tests, and maintainer state agree. It does not publish or tag anything.

## Scope

- Change the workspace application version from `0.2.0` to `0.3.0`, including
  workspace crate lock entries and every current application-facing assertion:
  `pangopup --version`, `pangopup -V`, `pangopup lookup --version`,
  `pangopup-build --version`, `/v1/status`, executable smoke tests, current
  container tags, and active workflow metadata.
- Preserve immutable historical records and tests whose subject is the
  already-published `v0.2.0` release or Ticket 050/051 evidence. A historical
  `0.2.0` string is not a version-bump target merely because it exists.
- Rewrite `README.md` as a first-use guide organized in this order:
  1. what PangoPup does and lookup-first/model-fallback behavior;
  2. exact download, installed-disk, and measured-memory requirements;
  3. direct Linux install and asset sync;
  4. CLI scoring and result provenance;
  5. foreground HTTP service with complete `curl` examples;
  6. Docker on AMD64 and Apple Silicon;
  7. update and safe uninstall;
  8. limitations, security, licenses, and maintainer links.
- Keep the README at no more than 350 lines and 2,200 words. Remove repeated
  engineering history while retaining all executable first-use contracts.
- State exact authenticated sizes from Ticket 053:
  - SNV download 1,931,694,270 bytes and installed member 15,033,158,255;
  - runtime download 691,874,664 bytes and installed members 812,662,222;
  - combined download 2,623,568,934 bytes and installed core members
    15,845,820,477.
- Explain mmap plainly: the installed fixed-width index is intentionally larger
  than its compressed download; mapping 15 GB reserves address space but Linux
  reads only touched pages, and mapped/file-backed bytes are not 15 GB of Rust
  heap or permanently resident RAM.
- Report Ticket 053 as one warm-cache Ryzen/Linux observation, not a universal
  guarantee: fresh SNV CLI median/maximum elapsed and peak RSS; ready service
  PSS/RSS/high-water RSS; 1/10/100 SNV HTTP latency; uncached model latency;
  and fresh-service SQLite-hit latency. Link the retained report and recommend
  operational headroom rather than turning the observed peak into a hard
  minimum.
- Create `planning/artifacts/054-release-notes.md` for v0.3.0. Name uninstall,
  read-only status/help/sync improvements since v0.2.0, measured resource
  guidance, unchanged biological/assets contract, supported delivery forms,
  and known Apple CPU warning.
- Reconcile current-state documentation:
  - `AGENTS.md` and `planning/faq.md` must no longer say GHCR is unpublished;
  - `architecture/service.md` and `architecture/delivery.md` must distinguish
    current public v0.2.0 history from the prepared v0.3.0 candidate;
  - close `planning/issues/2026-07-24-runtime-asset-trust-and-durability.md`
    with concise evidence for held descriptors, coherent profile activation,
    durable public compiled assets, and explicit non-syncing service startup;
  - update `planning/frontier.md` to make publication/qualification—not README
    or measurement—the next outcome.
- Update `spec/readme-first-use.md`, `spec/cli.md`, `spec/container-image.md`,
  current version-sensitive Rust/shell tests, and
  `.github/workflows/publish-container.yml` to enforce the candidate. Do not
  weaken current install, scoring, HTTP, Docker, security, attribution, update,
  or removal checks merely to shorten the README.
- Do not rebuild or republish biological assets, create a Git tag/release,
  upload a package/image, or move `latest` in this ticket.

## Success Checklist

- `pangopup --version`, `pangopup -V`, `pangopup lookup --version`, and
  `pangopup-build --version` print the conventional `0.3.0` identities with
  successful exit status; `/v1/status` reports `0.3.0`.
- Workspace package/lock versions, current container workflow tags, active
  tests/specs, README pinned installer, and README image examples agree on
  `0.3.0`/`v0.3.0` while historical v0.2.0 publication evidence stays intact.
- README stays within 350 lines/2,200 words and its executable spec proves the
  exact disk/memory/mmap explanation plus complete Linux, CLI, HTTP, Docker,
  update, uninstall, security, and attribution paths.
- Every README performance claim is traceable to
  `planning/artifacts/053-current-runtime-resources.md`; it labels the host,
  warm page cache, sample count, and limitation.
- Release notes explicitly say application code changes but the immutable SNV,
  model, reference, mask, score semantics, and routing default do not.
- Stale current-state scan finds no claim that the public GHCR image is absent,
  the runtime trust issue is still open, or Ticket 053 measurement remains
  future work.
- `make lint`, `make test`, and `make spec` pass.

## Decisions

1. **Use semantic version `0.3.0`.** `0.3` is not the repository's accepted
   release grammar. This is an application release; biological assets keep
   their immutable independent identities.
2. **One version across executable and container.** Publishing the two from
   different source commits created avoidable v0.2.0 provenance ambiguity.
   Ticket 055 will publish both from the exact candidate commit prepared here.
3. **Observed resource table, not requirements theater.** Ticket 053 gives a
   trustworthy single-host warm baseline. The README will state those numbers
   and sensible free-disk guidance without claiming every host needs exactly
   the observed RSS or has the same latency.
4. **Shorten by moving history, not by deleting operational answers.** The
   README remains the copy/paste user guide. Architecture, provenance, and old
   release narratives stay linked in tracked documents rather than repeated.
5. **Prepare but do not publish.** Version changes, docs, and release notes are
   reversible repository work. Tagging, GitHub release creation, and GHCR tag
   mutation remain Ticket 055's separately reviewed public effect.

## Dependencies

Ticket 053.

## Notes

- Current clean starting commit is `12532db` after Ticket 053 cleanup.
- Ticket 053 final measurements are in
  `planning/artifacts/053-current-runtime-resources.{md,jsonl}`.
- Preserve historical `planning/artifacts/050-*`, `planning/artifacts/051-*`,
  their literal v0.2.0 runbook assertions, and immutable-publication facts.
- The `v0.3.0` URL and image examples will become public only through Ticket
  055. The candidate README must not claim publication evidence that has not
  yet happened; release wording may identify the exact next immutable version
  while the frontier states that public effects remain pending.

## Coordinator Authorship

Coordinator: Codex (`/root`), 2026-08-05. Drafted from the completed Ticket 053
measurements and the accepted four-outcome v0.3.0 plan.

## Independent Ticket Review

Reviewer: `/root/ticket054_design_review` (independent, read-only).

Verdict: ACCEPT. The reviewer confirmed the release-candidate/publication
boundary, historical-version preservation, resource-claim limits, README
coverage, and roadmap order. Implementation guardrails are to bump the active
production checker in `scripts/check-production-qualification.py`, preserve
the `v0.2.0` malformed-version fixture and Tickets 050/051/053 evidence,
describe only the CLI figure as lookup-only memory, keep v0.3.0 public commands
prospective until Ticket 055, and record that explicit user-run sync—not
automatic service synchronization—closed the runtime trust issue.

## Implementation Evidence

Developer: `/root/ticket054_implementation`, 2026-08-05.

- Changed the workspace and lockfile packages to `0.3.0`; updated active CLI,
  HTTP-status, production-checker, container-workflow, shell-test, and spec
  assertions. Direct probes confirmed `pangopup --version`, `pangopup -V`,
  `pangopup lookup --version`, and `pangopup-build --version` all report the
  conventional `0.3.0` identity.
- Replaced the 425-line README with a 269-line/1,398-word first-use guide. It
  records exact authenticated asset sizes, explains fixed-width mmap versus
  resident memory, traces the single-host measurements to Ticket 053, gives
  complete Linux/CLI/HTTP/Docker/update/uninstall instructions, and labels all
  v0.3.0 public commands prospective.
- Added `planning/artifacts/054-release-notes.md`, reconciled public-GHCR and
  candidate/history language in architecture and maintainer documents, closed
  the runtime trust issue with explicit-sync evidence, and made exact-commit
  publication plus independent qualification the next frontier.
- Preserved the malformed `../install.sh --version v0.2.0` fixture, the literal
  Ticket 050 runbook assertions in production qualification, and all Ticket
  050/051/053 retained artifacts unchanged.
- Focused evidence passed: `cargo fmt --check`; `git diff --check`; the exact
  CLI/build version probes; the HTTP status JSON unit test; `bash
  tests/container-tag-absence.sh`; `bash tests/container-delivery.sh`; `bash
  tests/executable-delivery.sh`; `bash tests/production-release-qualification.sh`;
  and Mustmatch over `spec/readme-first-use.md`, `spec/cli.md`,
  `spec/container-image.md`, and `spec/build-cli.md` (49 passed, 1 skipped).
  The coordinator retains ownership of the complete final gate.
- After the first adversarial code review, corrected every KiB-to-MiB value
  (CLI peak 12.0/12.3 MiB; service PSS 102.3/102.5 MiB, RSS 105.8/106.0 MiB,
  and high-water RSS 137.0/137.3 MiB), restored compact read-only Docker
  status/network-disabled lookup/running-container HTTP/update-and-replace
  examples and their spec assertions, labeled the shortened model JSON as
  abridged, and corrected the frontier's stale v0.1/current-main wording.
  The README remains bounded at 309 lines/1,513 words. The focused Mustmatch
  set again passed 49/49 with one intentional skip; container delivery,
  executable delivery, and production qualification scripts also passed.
- Renewed review found one remaining pre-conversion frontier summary; it now
  reports the measured 102.3 MiB median PSS consistently with the README and
  retained Ticket 053 KiB evidence.

## Adversarial Code Review

Reviewer: `/root/ticket054_code_review` (independent, read-only).

Initial verdict: REJECT. The reviewer found KiB values mislabeled as MiB,
useful Docker/update examples removed during shortening, an abbreviated JSON
record presented as exact, and one stale frontier sentence. The developer
converted all memory units correctly, restored compact read-only status,
network-disabled lookup, running-container HTTP, executable update, and Docker
replacement flows with specs, labeled the JSON abridged, and corrected the
frontier. A first re-review found one remaining `105 MiB PSS` decimal/binary
mix-up in the frontier; it was corrected to `102.3 MiB`. Final verdict: ACCEPT.
The reviewer confirmed all current/historical version boundaries, asset and
resource claims, README limits, operational examples, and stale-number scans.

## External Effect Evidence

Coordinator: not applicable

## Coordinator Final Check

Coordinator: Codex (`/root`), 2026-08-05.

- `make lint` — passed (format, Clippy, advisories, bans, licenses, sources).
- `make test` — passed, including executable-delivery and production-release
  qualification scripts; only declared retained-production measurements were
  skipped.
- `make spec` — 276 passed, 7 skipped.
- Direct version probes and `/v1/status` assertions report `0.3.0`.
- `git diff --check` — passed.
- README is 309 lines and 1,513 words, below the 350/2,200 limits.
- Stale scan confirms current documents no longer call GHCR unpublished, the
  runtime trust issue is closed, resource evidence is established, and public
  v0.2.0 history remains distinct from the unpublished v0.3.0 candidate.
- No tag, GitHub release, package upload, image tag, `latest` mutation, or
  biological asset effect occurred.
