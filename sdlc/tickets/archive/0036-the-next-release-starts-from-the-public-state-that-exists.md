---
flow: build
priority: 10
---
# The next release starts from the public state that exists

GitHub Latest is immutable executable release v0.4.0 at `ea4438e50762e32f09052b364060c89201ed78bc`. Its six executable files are public and qualified. Full uninstall then failed against the normal read-only installed profile. Publication stopped before the container moved, so GHCR `latest`, `0.3.0`, and `v0.3.0` still identify index `sha256:5d00753e9b5019e0408fd33ca39371684c1eebb38b3f559e2b4f953ce062bcc0`, while GHCR `0.4.0` and `v0.4.0` remain absent. The uninstall defect and the release-gate defects found during qualification are fixed on reviewed `main`.

The repository must prepare one coherent v0.4.1 executable and container candidate from the fixed source. Its version gate and current-state documents must represent the public executable and public container versions separately until v0.4.1 completes. The public v0.4.0 release body must remain byte-identical. Its publication record must stop claiming that no v0.4.0 release exists and must record the executable-only outcome and the reason container publication stopped. A new release note and fail-closed publication record must govern v0.4.1 from the exact live predecessor state.

Done, observably:

- The workspace, lockfile packages, executable and service expectations, qualification checks, citation metadata, installer examples, and container workflow identify candidate v0.4.1.
- The version gate distinguishes public executable v0.4.0 from public container v0.3.0 and rejects drift in either fact.
- Current-state architecture and planning documents identify the executable-only v0.4.0 outcome, the unchanged public v0.3.0 container index, and the v0.4.1 candidate.
- The exact v0.4.0 release-note body remains unchanged and protected as immutable publication history.
- The v0.4.0 publication record pins immutable release ID `383614742`, Latest status, non-draft and non-prerelease state, exact target and direct tag resolution to `ea4438e50762e32f09052b364060c89201ed78bc`, and every public executable member name, size, and SHA-256. It also pins the absent v0.4.0 container aliases, the unchanged v0.3.0 container predecessor, and the verified full-uninstall stop condition.
- New v0.4.1 release notes state that scoring assets and the HTTP, JSON, and scoring contracts do not change. They describe the read-only full-uninstall fix and the qualification-gate portability fixes.
- A new prepared v0.4.1 publication record requires an out-of-commit authorization receipt or user message that binds Ian Maurer's explicit 2026-09-06 authorization to the final `origin/main` publication commit and actual UTC citation date before any remote mutation. The prepared commit does not attempt to contain its own hash. The completed record adds the observed binding afterward. Any date or commit change stops publication, requires a new candidate commit, repeats every commit-bound qualification, and requires a new authorization binding that names the replacement commit before any public effect.
- The v0.4.1 record authenticates current `origin/main` and its successful build runs before staging. It binds one retained stage receipt and two native leaf digests to the publication commit while every user-facing v0.4.1 container alias remains absent. It admits one exact private six-file executable inventory and compares the draft target commit, title, release-note body, member names, sizes, and SHA-256 values with the admitted inputs before publication.
- Immediately before executable publication, the v0.4.1 record requires GitHub Latest to remain immutable v0.4.0 at its pinned commit and GHCR `latest` to remain the pinned v0.3.0 index. Immediately before index creation, it instead requires GitHub Latest to be the newly verified immutable v0.4.1 executable at the publication commit while GHCR `latest` still remains the pinned v0.3.0 index. Anonymous post-publication checks require an immutable Latest non-draft non-prerelease executable release, exact target and direct tag resolution to the publication commit, byte-identical release-note body, and the exact six admitted member names, sizes, and SHA-256 values.
- The v0.4.1 record qualifies the public installer plus code-only and full uninstall before container finalization. Final anonymous verification proves that the public index contains exactly the held AMD64 and ARM64 leaf digests and that all v0.4.1 aliases resolve to that index.
- Mechanical checks protect the split predecessor facts, historical v0.4.0 body and complete partial record, v0.4.1 release inputs, authorization and date binding, private-draft bindings, public executable verification, every other ordered publication gate, tag absence, and credential prohibition. No command, mutation, or publication step may intervene between the final pre-publish checks and executable publish request or between the final pre-index checks and index creation. Mutation tests that remove, reorder, or insert an intervening step fail the normal test suite.
- Mutation checks prove that stale candidate claims, either side of the split predecessor, changed v0.4.0 history, or incomplete v0.4.1 release evidence fail the normal gate.
- The normal lint, test, and executable specification gates pass.

Boundary: Do not change product behavior, HTTP or command-line contracts beyond the software version, scoring identity inputs beyond that version, scoring assets, dependency versions, fixed identity fixtures, prior release bodies, or archived SDLC history. Do not dispatch a workflow or create, edit, delete, publish, or retag any release, executable asset, container alias, staged leaf, or scoring asset in this ticket.
