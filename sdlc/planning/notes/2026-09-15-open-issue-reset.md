# Open issue reset after the 0.5 lint fix

Date: 2026-09-15

Ian decided to park exhaustive and observed-indel precomputation. The chromosome 22 recurrence result remains evidence, but catalogue membership does not supply a score. This decision accepts slow first calls that the current SNV index and exact cache cannot answer. Ian can reopen precomputation after full-genome traffic and scoring-cost evidence.

Ian also parked accelerator-throughput work tied to that programme. CPU model-miss speed remains an open performance goal. The exact loss-only route remains a narrow candidate because the mask can prove zero loss for many requests without model inference.

Broad threshold triage and a second classifier have no active build path. The bounded SNV proxy pilot measured potential lookup coverage but contained no model result at or above 0.10. It cannot qualify safe dismissal. PangoPup does not own a clinical cutoff. Callers can apply their own thresholds to exact returned scores. A later threshold-only contract needs model-positive validation and a downstream use case before the issue reopens.

The earlier scope treated third-decimal model output as a high-priority draft candidate after lint cleanup. The published SNV dataset contains hundredths only. Model output retains finer values internally. The superseding decision below rules out route-only precision. No source here establishes clinical meaning at that resolution.

The earlier scope placed index-size reduction after precision. The measured sparse candidate suggests a large disk reduction, but the complete format and hardened reader do not exist. It needs a built size, runtime latency, and bundle migration decision. The superseding 0.5 scope below makes a qualified smaller asset a release requirement.

`d101aa0` updated the locked TLS dependency and restored the local `make lint` gate. Full macOS test and spec portability remain separate gate work.

## Superseding 0.5 scope decision, 2026-09-15

Ian requires equal reported precision on the SNV index and model routes. The official Zenodo v1 source build accepted all 4,099,255,665 rows through a parser that rejects third-decimal scores. It did not discard a third digit. The current index cannot supply one. Keep both routes at two decimals and defer the model-only precision draft. Ian can reopen precision if a verified three-decimal source and reproducible model output support both routes.

Ian also selected a new, qualified smaller SNV asset as a 0.5 requirement. This supersedes the earlier statement that index-size work cannot gate 0.5. The installed-size and lookup-latency limits must be declared before qualification. A new SNV asset changes the runtime profile and requires an asset sync. Update the 0.5 consumer guidance when the asset passes. Mac test and spec gates must pass before the release is ready. Publishing itself remains normal release work.
