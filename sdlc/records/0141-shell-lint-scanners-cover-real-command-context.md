# Shell lint scanners cover real command context

PangoPup's shell gates now share one bounded scanner. Discovery reads Git's exact tracked regular-file set, includes every `.sh` path and admitted POSIX `sh` or Bash shebang, and excludes checked output. The lexer preserves token kind, raw quoting, and executable expansions. It scans command and process substitutions inside parameter expansion, arithmetic, and unquoted heredocs while leaving quoted heredocs and literal text inert.

The negative-assertion gate distinguishes final unasserted negation from conditions, explicit failure branches, and nonfinal boolean lists. The builder gate recognizes all three admitted function declarations, follows only proven calls, carries target uses and substitutions inside called functions, and invalidates bindings on assignment, `unset`, and `read`. Single-quoted variable literals, uncalled functions, late builds, and unproven control flow earn no build evidence. The pipeline gate follows grouped and subshell right-hand stages and recognizes quiet grep, `head`, `sed q`, exit-on-first-match awk, `grep -m`, `grep -l`, and bare `read` across the admitted option forms.

The coverage gate holds exact miniature Git discovery, 118 named positive, refusal, counterexample, and repository-mode mutations, a real 20,000-row full-read operation beside its executed defective `head` mutation, and malformed-syntax refusal in all three repository modes. All 16 live early-reader pipelines were removed or rewritten while preserving their question and positive control.

SDLC ticket 0249 supplied the canonical lifecycle repair. PangoPup adopted `before`, `failure`, `health`, `success`, `tasks`, and `provenance.json` byte for byte from SDLC main `eea22bf014865252511a9a4bb9e4917ae9657017`. The manifest records source provenance commit `f17578cb4e24d824c4663e524537fd77d1bc4ffe`. Its SHA-256 is `17022911ff318dd755d2428c3ae6c7acd0f20ccc2d24c8a550c4e8be3cdb739e`; `failure` is `a2be0f6f787d3610468e248727c78358e20f6408a631d180d99fc24df8a23d28`; `health` is `47fc5e82d1a0840ff3a94bc5c6c3e7a90c77bdcc127ce217bd2ca508f0c01567`. All five scripts retain mode `100755`; the manifest retains `100644`. Comparison against the accepted SDLC checkout returned equal for all six files. No Factory command or deployment ran.

The scanner does not execute expansion, follow `eval`, infer generated shell, or prove arbitrary control flow. Those forms supply no command evidence. Command and parameter expansion nesting stops with a named diagnostic beyond 64 levels. Function calls and summaries stop beyond 128 levels. Builder analysis memoizes function-effect summaries and caps both function expansion and processed commands in proportion to the parsed command count. The supported grammar and limits are recorded in `tests/support/shell-scan.md`.

The final candidate passed `make lint`, `make test`, `make spec`, and `git diff --check` on macOS. The specification gate ran 205 blocks. A temporary index staged the complete candidate without changing the worktree index. The three repository modes then scanned 69 shell files, including `tests/shell-scanner-coverage.sh`, and reported four guarded executable users, zero bad or unsupported builder flows, 114 negations with zero unsafe or unsupported forms, and 9,750 pipelines with zero findings or unsupported forms.

Independent code review accepted the final implementation after four adversarial rounds. The accepted probes include quoted command paths, called-function binding changes, nested declarations, literal and executable substitutions in parameter expressions and heredocs, grouped readers, option arguments, 1,050-function chains, and a 300-by-300 repeated-body work bound. Drafts 0098, 0110, 0114, and 0115 are archived with this ticket.
