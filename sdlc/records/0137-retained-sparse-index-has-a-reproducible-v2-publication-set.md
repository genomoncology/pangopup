# The retained sparse index has a reproducible v2 publication set

Two independent absent-root runs reproduced the complete local `snv-grch38-v2` publication set from a standalone clean detached clone of commit `8eb8917ff9f788c28b29287ed3a06b6b4762c554`. A digest-pinned Linux ARM64 Debian 13 container built `pangopup-build` and `pangopup` with Rust 1.93.1, `cargo build --locked --release`, read-only source and inputs, and a fresh external target. The executables have SHA-256 values `53d7010273c1e34d7813b16b032bcb3a95edd40aba39e4828ab2550bccec2777` and `28957de12148a61d8d1e451c8e0c17286784a9cc3ff215ab437683151426d48c`.

Both runs authenticated the 2,035,371,437-byte candidate, its report, and the immutable fixed-v1 authority. They independently assembled, certified, packed, verified, reconstructed, certified again, prepared, and installed under isolated roots. They reproduced bundle `sha256:17085bb737bd3a9e54df9cb60d643c8ad8b8b6cb2c5bf0dbe4fc4c1e95c2b4f7` and transport `sha256:e2a9090a71c4fb68dfcf496e5389db2b29b767a5b95d8fa50acdf2b75458be0e`. The transport has two parts totaling 1,310,940,560 compressed bytes. The installed three-file bundle totals 2,035,377,070 bytes. A fresh download plus installed members totals 3,346,324,528 bytes.

The cross-run comparison checked 55 selected files and found identical member sets, bytes, sizes, and SHA-256 values. Both isolated installs reported `partial`, with the exact v2 SNV bundle ready and runtime missing. In each run, 994 explicit requests matched the accepted fixture oracle after the documented identity and checked-name normalization, and isolated discovery produced byte-identical output. Six explicit position-1 misses returned the checked `not_found` rows. Each discovered miss exited 2 with empty stdout and the exact `MODEL_REJECTED` reference-context error. The combined batch stopped at the first rejection without partial output. This observed behavior corrected the reviewed ticket assumption without a product change.

The existing installation's 35-entry before and after snapshots remained byte-identical with SHA-256 `721fa9e7c81769fa101f5ea74c4647a60ae75ad1073d6cf98a204d03641a04e5`. The comparison includes paths, modes, owners, sizes, content hashes, link identities, and modification metadata. It excludes access timestamps. Postflight also reauthenticated the candidate and report and reconfirmed the source clone.

Run A remains complete under `data/pangopup/sparse-release-v2-2026-09-19`. The retained evidence records the exact image, platform, Rust versions, commands, executable hashes, inputs, logs, oracle checks, comparisons, and snapshots. After comparison, the task removed only Run B's recorded duplicate bundle, transport, reconstructed bundle, and isolated install. Run B retains its generated release files and qualification evidence.

The checked proof, profile, checksum list, and notes are the exact generated Run A bytes. They land after their recorded target commit. No GitHub asset was published, no production or current installation changed, and no compiled authority or active profile selects v2. Command and service parity, unfiltered production latency, complete-file throughput, runtime binding, upgrade, and rollback remain activation gates.

Independent design review rejected the first scope because no unfiltered performance gate or complete cross-format route harness exists. It accepted the bounded packaging scope after the ticket deferred those activation claims. The retained run disproved the reviewer's missing-runtime expectation for the six position-1 misses; request validation correctly returns `MODEL_REJECTED` first, and the reviewer accepted the corrected evidence. Independent results review directly checked provenance, inventories, hashes, snapshots, lookup outputs, authorities, sizing, and duplicate removal and accepted the result. It found and corrected one stale frontier sentence.

`make lint`, `make test`, `make spec`, and `git diff --check` passed after the small authorities and records were added. The specification gate ran 199 blocks.
