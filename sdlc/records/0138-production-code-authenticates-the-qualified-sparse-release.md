# Production code authenticates the qualified sparse release

PangoPup now parses the v2 proof and release profile through closed version-specific types and authenticates only the exact checked 5,405-byte proof and 4,755-byte profile. Validation binds the immutable v1 corpus facts, sparse provenance, candidate and tooling commits, bundle and transport members, compression facts, literal URLs, proof link, ordered sizing members, and checked sums. Exact independent length and digest pins reject self-consistent forged pairs. V1 bytes, parsing, preparation, errors, and the ordinary production selector remain unchanged.

An internal inactive preparation path opens and exhaustively certifies a real sparse bundle before matching its held identity and member facts to the qualified authority. It then derives a canonical inner runtime profile while preserving every model-side field. With software version and CPU policy fixed, miniature evidence changes only the SNV descriptor and derived runtime-profile, scoring, and data-set identities while fixed and sparse lookups remain exact. Trusted production admission still rejects that sparse profile. No sync, discovery, release, installation, service, or HTTP path selects it.

Independent design review corrected the existing-state description, separated inner profile derivation from the outer runtime release, and bounded the identity claim. Independent code review rejected the first implementation because it accepted fabricated metadata and malformed tests stopped at size limits. The accepted remediation certifies the actual bundle, confines miniature authority injection to tests, exercises named parser failures under bounds, and rejects a recomputed forged pair through independent pins. `make lint`, `make test`, `make spec`, and `git diff --check` passed on macOS. The specification gate ran 200 blocks.
