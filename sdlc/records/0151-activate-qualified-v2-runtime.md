# Activate the qualified v2 runtime

The release branch selects the public `snv-grch38-v2` and `runtime-grch38-v2` authorities for ordinary sync and scoring. The fixed-v1 assets remain readable for explicit rollback. Ticket 0150 records the public asset inventory. This record covers activation, not the v0.5.0 software release.

The exact pushed branch commit `eaeac52ad6ec862860e8743ad6e4750afb8379a8` passed Linux and macOS GitHub checks in run `35865906057`. Local `make lint`, `make test`, and `make spec` passed before public qualification. The independent qualification used a binary with SHA-256 `8828fc4236213f55557a9fe25ca9136325611c59cb98e419609ac1987a93cc0e` from that branch.

Fresh anonymous ordinary `sync --progress` downloaded 2,002,822,127 bytes into an isolated installation. Status selected SNV bundle `17085bb7…`, runtime profile `ce91b332…`, and runtime transport `1c893bd4…`. A second `sync --offline` reused both components with zero downloaded bytes. CLI lookup returned `0.00/0.00`; a modeled insertion returned `0.00/−0.09`. Live loopback HTTP returned both routes with v2 data-set identity `5ffac268…`, scoring identity `0dd20757…`, and exact two-decimal scores. The service exited cleanly.

A private copy-on-write clone of the retained fixed-v1 installation began with coherent SNV `c4c4162b…` and runtime `0efc5b7d…`. Ordinary offline sync staged v2 while status and lookup continued to use v1. Completion selected coherent v2 without network transfer. Explicit v1 SNV staging left v2 active until v1 runtime installation atomically restored the coherent v1 pair. V1 lookup passed after rollback; both versions' bundles and profiles remained readable. The model cache retained one entry and `next_write_sequence=2` before and after v2 scoring, rollback, and a repeated model request. All 17 retained v1 installation files matched the prior snapshot. Ticket 0140 separately records the real public v0.4.1 layout-1 to candidate v0.5.0 layout-2 cache transition; this run did not repeat that transition.

The qualification used private installation and cache paths and made no production selection change. Its files were moved recoverably to `/Users/ian/.Trash/pangopup-0151-public.wdWiiCS4`; the temporary path is absent and no test service remains. The production selector changes only when the future v0.5.0 executable or container is published and installed. No score precision or indel policy changed.

Integration CI on commit `691153153c7026741731cd6623c1235fcc4a8146` exposed a service shutdown race in the Linux native fixture. After a successful listening event and status response, immediate SIGTERM sometimes retained the default process action and ended with signal 15. The service installed its Tokio signal handler only when its shutdown future was first polled. Ticket branch `003e503` moved handler registration before the listening event and added an immediate-SIGTERM lifecycle test. That test failed with signal 15 and empty standard error before the change, then passed after the change and on ten repeated runs. The full lifecycle suite passed 24 tests with two retained-asset tests ignored; first- and second-signal behavior remained green. The branch passed `make lint`, `make test`, and `make spec` with 207 specifications before integration.
